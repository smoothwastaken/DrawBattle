import React from "react";

import { motion, AnimatePresence } from "framer-motion";

const results = () => {
  return (
    <AnimatePresence>
      <div></div>
    </AnimatePresence>
  );
};

export default results;
